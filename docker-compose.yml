name: harina-v4

services:
  receipt-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./secrets:/app/secrets:ro
